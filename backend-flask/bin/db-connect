#!/usr/bin/env bash
SCRIPT_DIR="$(dirname "$(realpath "$0")")"
PROJECT_ROOT="$(realpath "$SCRIPT_DIR/../../")"
if [ -f "$PROJECT_ROOT/.env" ]; then
  set -a
  source "$PROJECT_ROOT/.env"
  set +a
fi

CYAN='\033[1;36m'
NO_COLOR='\033[0m'
printf "${CYAN}== db-connect${NO_COLOR}\n"

psql "$CONNECTION_URL"
